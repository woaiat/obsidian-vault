!(Linux 综合模拟试卷 50题)



**规则：**
*   **时间限制**：40 分钟。
*   **作答方式**：你可以分批作答（比如先做 1-25，再做 26-50），或者一次性做完。
*   **做完后回复“交卷”**，我会给出带详细解析的成绩单。

---

### **Linux Comprehensive Final Exam (50 Questions)**

#### **Part 1: OS Basics & File System (Chapter 1 & 2)**

1.  **Which component serves as the interface between the user and the kernel?**
    A. Hardware
    B. Shell
    C. Utility
    D. Daemon

2.  **Linux is classified as which type of operating system?**
    A. Single-user, Single-tasking
    B. Single-user, Multitasking
    C. Multiuser, Multitasking
    D. Multiuser, Single-tasking

3.  **The Linux kernel was created by Linus Torvalds in which year?**
    A. 1969
    B. 1983
    C. 1991
    D. 1995

4.  **Which directory typically contains device files (e.g., hard drives)?**
    A. `/bin`
    B. `/dev`
    C. `/etc`
    D. `/lib`

5.  **Which command displays the current absolute path of your working directory?**
    A. `ls`
    B. `cd`
    C. `pwd`
    D. `path`

6.  **Which symbol represents the parent directory?**
    A. `.`
    B. `..`
    C. `~`
    D. `/`

7.  **In the permission string `drwxr-xr--`, what permission does the "Group" have?**
    A. Read, Write, Execute
    B. Read, Execute
    C. Read Only
    D. No permission

8.  **Which command is used to remove a directory that contains files?**
    A. `rmdir dir`
    B. `rm -r dir`
    C. `del dir`
    D. `rmdir -r dir`

9.  **Which `ls` option displays file type indicators (e.g., `/` for directories)?**
    A. `-l`
    B. `-a`
    C. `-F`
    D. `-R`

10. **The numeric equivalent of the permission `r-x` is:**
    A. 3
    B. 4
    C. 5
    D. 6

---

#### **Part 2: Text Editors (Vi/Vim) (Chapter 3)**

11. **Which mode does Vi start in by default?**
    A. Insert Mode
    B. Command Mode
    C. Last Line Mode
    D. Visual Mode

12. **In Vi Command Mode, which key moves the cursor DOWN?**
    A. h
    B. j
    C. k
    D. l

13. **Which command saves the file and quits Vi?**
    A. `:q`
    B. `:w`
    C. `:wq`
    D. `:exit`

14. **To delete the current line in Vi, you press:**
    A. `dd`
    B. `dl`
    C. `dx`
    D. `rm`

15. **Which command allows you to search for a pattern *forward* in Vi?**
    A. `?pattern`
    B. `/pattern`
    C. `:pattern`
    D. `s/pattern`

16. **How do you undo the last change in Vi?**
    A. Press `Ctrl+Z`
    B. Press `u`
    C. Type `:undo`
    D. Press `U`

17. **Which command appends text *after* the cursor position?**
    A. `i`
    B. `a`
    C. `o`
    D. `I`

---

#### **Part 3: Filters, Pipes & Redirection (Chapter 4)**

18. **Which file descriptor represents Standard Error (stderr)?**
    A. 0
    B. 1
    C. 2
    D. 3

19. **Which operator redirects output to a file, *appending* to it?**
    A. `>`
    B. `>>`
    C. `<`
    D. `|`

20. **Which command counts the number of lines in a file?**
    A. `wc -c`
    B. `wc -w`
    C. `wc -l`
    D. `count -l`

21. **Which command is used to extract specific columns from a file?**
    A. `grep`
    B. `cut`
    C. `paste`
    D. `tr`

22. **The command `ls | grep "txt"` is an example of using a:**
    A. Redirection
    B. Pipe
    C. Filter
    D. Alias

23. **Which `grep` option performs a case-insensitive search?**
    A. `-v`
    B. `-c`
    C. `-n`
    D. `-i`

24. **The command `tr 'a-z' 'A-Z' < file` does what?**
    A. Sorts the file alphabetically.
    B. Converts lowercase to uppercase.
    C. Deletes all lowercase letters.
    D. Counts uppercase letters.

25. **Which command allows you to view output on the screen AND save it to a file simultaneously?**
    A. `cat`
    B. `more`
    C. `tee`
    D. `echo`

---

#### **Part 4: Shell Scripting (Chapter 5 & 6)**

26. **Which line must appear at the beginning of a Bash script?**
    A. `//bin/bash`
    B. `#!/bin/bash`
    C. `using bash`
    D. `include bash`

27. **How do you assign the value "Steve" to variable `NAME`?**
    A. `NAME = "Steve"`
    B. `NAME="Steve"`
    C. `$NAME="Steve"`
    D. `set NAME "Steve"`

28. **Which command executes a script in the *current* shell environment?**
    A. `./script.sh`
    B. `bash script.sh`
    C. `sh script.sh`
    D. `source script.sh`

29. **In a shell script, `$1` represents:**
    A. The script name
    B. The first argument
    C. The process ID
    D. The exit status

30. **Which operator is used to check if two integers are *equal* in a `test` command?**
    A. `==`
    B. `=`
    C. `-eq`
    D. `-equal`

31. **Which conditional statement ends with `fi`?**
    A. `case`
    B. `for`
    C. `if`
    D. `while`

32. **What is the correct syntax for command substitution?**
    A. `date`
    B. `$(date)`
    C. `${date}`
    D. `(date)`

33. **Which test operator checks if a *file exists*?**
    A. `-f`
    B. `-d`
    C. `-e`
    D. `-x`

34. **The `read` command is used to:**
    A. Read file permissions.
    B. Get input from the user.
    C. Display file content.
    D. Check if a file is readable.

35. **In a `case` statement, which pattern matches *anything* (default case)?**
    A. `*`
    B. `?`
    C. `default`
    D. `all`

---

#### **Part 5: Process & File Management (Chapter 7)**

36. **Which command searches for files in a directory hierarchy based on criteria like name or size?**
    A. `locate`
    B. `grep`
    C. `find`
    D. `whereis`

37. **The `find` command option `-mtime -7` finds files modified:**
    A. Exactly 7 days ago.
    B. Within the last 7 days.
    C. More than 7 days ago.
    D. 7 minutes ago.

38. **Which command displays a snapshot of current processes?**
    A. `top`
    B. `ps`
    C. `jobs`
    D. `kill`

39. **Which signal is sent by `kill -9`?**
    A. SIGTERM (Terminate)
    B. SIGKILL (Force Kill)
    C. SIGHUP (Hangup)
    D. SIGSTOP (Pause)

40. **How do you run a command in the background?**
    A. Add `&` at the end.
    B. Add `bg` at the beginning.
    C. Press `Ctrl+Z`.
    D. Press `Ctrl+C`.

41. **The PID of the `init` (or `systemd`) process is always:**
    A. 0
    B. 1
    C. 100
    D. Random

42. **Which command brings a background job to the foreground?**
    A. `bg`
    B. `jobs`
    C. `fg`
    D. `front`

---

#### **Part 6: Utilities & Administration (Chapter 8)**

43. **Which command is used to create a compressed `.gz` file?**
    A. `tar`
    B. `zip`
    C. `gzip`
    D. `compress`

44. **Which `tar` options are used to *create* a compressed archive (`.tar.gz`)?**
    A. `-cvf`
    B. `-xvf`
    C. `-czvf`
    D. `-xzvf`

45. **Which command allows a root user to add a new user to the system?**
    A. `adduser`
    B. `useradd`
    C. `newuser`
    D. `createuser`

46. **Which file stores user password information (encrypted)?**
    A. `/etc/passwd`
    B. `/etc/shadow`
    C. `/etc/group`
    D. `/etc/security`

47. **To delete a user AND their home directory, which option is used with `userdel`?**
    A. `-d`
    B. `-f`
    C. `-h`
    D. `-r`

48. **Which command is used to change the group ownership of a file?**
    A. `chmod`
    B. `chown`
    C. `chgrp`
    D. `groupmod`

49. **Which file is used for local DNS name resolution?**
    A. `/etc/resolv.conf`
    B. `/etc/hosts`
    C. `/etc/network`
    D. `/etc/dns`

50. **Which `chmod` command sets permissions to `rwxr-xr-x`?**
    A. `chmod 777 file`
    B. `chmod 755 file`
    C. `chmod 644 file`
    D. `chmod 750 file`

---

**加油！做完请告诉我！**