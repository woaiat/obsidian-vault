

这份试卷严格基于你提供的 **PDF (Chapter 1-8)**，难度略高于普通及格线，旨在帮你冲击满绩。

**考试模拟规则：**
*   **时间**：建议 **60-90 分钟** 完成。
*   **环境**：闭卷，关闭所有笔记和终端。
*   **心态**：把它当做真题来做。

---

### **Linux 期末综合模拟试卷 (Final Simulation)**

**适用教材**：Based on provided Course PDF
**满分**：100分

---

#### **一、单项选择题 (Single Choice, 20题, 每题1.5分, 共30分)**

1.  **Linux 被定义为哪种类型的操作系统？**
    A. Single-user, Single-tasking
    B. Single-user, Multitasking
    C. Multiuser, Multitasking
    D. Multiuser, Single-tasking

2.  **负责在用户和内核之间进行翻译和交互的组件是：**
    A. Hardware
    B. Shell
    C. Utility
    D. Daemon

3.  **在 Linux 文件系统中，代表“当前用户的家目录”的符号是：**
    A. `/`
    B. `.`
    C. `..`
    D. `~`

4.  **若文件权限显示为 `drwxr-xr--`，则 Group（所属组）的权限是：**
    A. 读、写、执行
    B. 读、执行
    C. 只读
    D. 无权限

5.  **如果你想查看名为 `.bashrc` 的隐藏文件，应该使用哪个命令？**
    A. `ls -l`
    B. `ls -a`
    C. `ls -R`
    D. `ls -F`

6.  **在 Vi 编辑器的命令模式下，哪两个键分别用于“删除整行”和“撤销”？**
    A. `dd`, `u`
    B. `yy`, `p`
    C. `d`, `ctrl+z`
    D. `x`, `r`

7.  **Vi 编辑器中，强制退出且不保存的命令是：**
    A. `:w`
    B. `:q`
    C. `:wq`
    D. `:q!`

8.  **下列哪个文件描述符 (File Descriptor) 代表标准错误输出 (stderr)？**
    A. 0
    B. 1
    C. 2
    D. 3

9.  **想要统计一个文件中有多少行，应使用命令：**
    A. `wc -c`
    B. `wc -w`
    C. `wc -l`
    D. `count -l`

10. **命令 `sort -n file.txt` 的作用是：**
    A. 按 ASCII 字符排序
    B. 按数值大小排序
    C. 倒序排列
    D. 忽略大小写排序

11. **Shell 脚本的第一行（Shebang）通常是：**
    A. `//bin/bash`
    B. `#!/bin/bash`
    C. `rem /bin/bash`
    D. `#include <bash>`

12. **在脚本中，哪个变量代表命令行参数的总个数？**
    A. `$0`
    B. `$1`
    C. `$#`
    D. `$@`

13. **`test` 命令中，用于判断“两个整数不相等”的操作符是：**
    A. `!=`
    B. `-ne`
    C. `-nq`
    D. `-not`

14. **若要执行一个名为 `script.sh` 的脚本，且该脚本没有执行权限，应使用：**
    A. `./script.sh`
    B. `run script.sh`
    C. `bash script.sh`
    D. `exec script.sh`

15. **`find` 命令中，查找“7天以内修改过”的文件的参数是：**
    A. `-mtime 7`
    B. `-mtime +7`
    C. `-mtime -7`
    D. `-atime 7`

16. **能够实时显示系统进程动态（如 CPU 占用率）的命令是：**
    A. `ps`
    B. `top`
    C. `jobs`
    D. `pstree`

17. **发送什么信号 (Signal) 可以强制杀死一个卡死的进程？**
    A. -15 (SIGTERM)
    B. -9 (SIGKILL)
    C. -1 (SIGHUP)
    D. -2 (SIGINT)

18. **`tar` 命令中，用于“解包”的参数是：**
    A. `-c`
    B. `-x`
    C. `-v`
    D. `-f`

19. **用于修改用户密码（需要 root 权限修改他人）的命令是：**
    A. `useradd`
    B. `usermod`
    C. `passwd`
    D. `chpasswd`

20. **`/etc/hosts` 文件的主要作用是：**
    A. 存储用户密码
    B. 本地 DNS 映射
    C. 配置网卡 IP
    D. 存储系统日志

---

#### **二、多项选择题 (Multiple Choice, 5题, 每题3分, 共15分)**
*(少选得1分，错选不得分)*

21. **以下哪些是 Linux 系统中的有效文件类型？**
    A. Ordinary Files (普通文件)
    B. Directory Files (目录文件)
    C. Block Device Files (块设备文件)
    D. Registry Files (注册表文件)

22. **关于 Vi 编辑器的模式，以下说法正确的是：**
    A. 默认打开时处于 Input Mode。
    B. 按 `i`, `a`, `o` 可以从 Command Mode 进入 Input Mode。
    C. 按 `Esc` 键可以返回 Command Mode。
    D. 按 `:` 键进入 Last Line Mode。

23. **以下哪些符号用于输出重定向？**
    A. `>`
    B. `<`
    C. `>>`
    D. `|`

24. **在 Shell 脚本中，以下哪些循环结构是有效的？**
    A. `for ... do ... done`
    B. `while ... do ... done`
    C. `repeat ... until`
    D. `until ... do ... done`

25. **关于 `gzip` 压缩命令，以下描述正确的是：**
    A. 它可以压缩整个目录为一个文件。
    B. 压缩后原文件会被替换为 `.gz` 文件。
    C. 使用 `-d` 参数可以解压缩。
    D. 使用 `-l` 参数可以查看压缩文件信息。

---

#### **三、填空题 (Fill in the Blanks, 10题, 每题1.5分, 共15分)**

26. 权限 `rwxr-x---` 对应的八进制数字（Numeric Mode）是 ________。
27. 在 Vi 中，想要显示行号，应在末行模式输入命令 ________。
28. 要将 `ls` 命令的**错误信息**丢弃（不显示），应将其重定向到 ________。
29. 命令 `chmod u+x script.sh` 的作用是给 ________ (Owner/Group/Others) 增加执行权限。
30. 在 Shell 脚本中，引用变量 `name` 的值的语法是 ________。
31. `if` 语句块结束的关键字是 ________；`case` 语句块结束的关键字是 ________。
32. `find` 命令中，用于对找到的每一个文件执行特定操作的参数是 ________。
33. 要把一个在前台运行的命令暂停并挂起到后台，应按快捷键 ________。
34. 创建压缩归档文件 `data.tar.gz` 的 `tar` 命令组合参数（4个字母）是 ________。
35. 存放系统用户账号基本信息（如 UID, GID, Home 目录）的文件是 ________。

---

#### **四、判断题 (True/False, 10题, 每题1分, 共10分)**

36. `mv` 命令既可以移动文件，也可以重命名文件。 ( )
37. 在 Linux 中，`rmdir` 命令可以删除包含文件的非空目录。 ( )
38. `grep` 命令默认是区分大小写的，使用 `-i` 可以忽略大小写。 ( )
39. 执行脚本时，`source script.sh` 会在当前 Shell 环境中执行，而 `./script.sh` 会在子 Shell 中执行。 ( )
40. `test -d filename` 用于判断 filename 是否为一个普通文件。 ( )
41. 管道符 `|` 的作用是将前一个命令的标准输出 (stdout) 作为后一个命令的标准输入 (stdin)。 ( )
42. `kill` 命令默认发送的信号是 SIGKILL (-9)。 ( )
43. `usermod -G groupname username` 会将用户添加到新组，并保留该用户原有的其他附加组。 ( )
44. `/bin` 目录通常存放系统启动所需的必要二进制命令，如 `ls`, `cp`。 ( )
45. `locate` 命令比 `find` 命令慢，因为它是实时搜索硬盘的。 ( )

---

#### **五、综合大题 (Subjective Questions, 3题, 共30分)**

**46. 概念解释题 (5分)**
请简述 **Absolute Path (绝对路径)** 和 **Relative Path (相对路径)** 的区别，并各举一个例子。

**47. 命令组合题 (10分)**
假设当前目录下有一个文件 `students.txt`，内容格式为 `Name:ID:Score` (例如 `Alice:101:90`)。请写出完成以下任务的命令：
1.  查看该文件的前 5 行。
2.  将文件按分数（第3列）进行**数值倒序**排序。
3.  查找所有包含 "Smith" 的行，并统计行数。
4.  将该文件的拥有者权限设置为“读写”，同组用户为“只读”，其他人“无权限”（请用数字法 chmod）。

**48. 编程脚本题 (15分)**
编写一个 Shell 脚本 `file_checker.sh`，要求实现以下功能：
1.  脚本接收**一个参数**（文件名）。
2.  判断用户是否输入了参数，如果没有，输出 "Usage: ./file_checker.sh filename" 并退出。
3.  判断该参数指定的文件是否存在：
    *   如果存在，输出 "File exists."，并判断它是否是**目录**。如果是目录，输出 "It is a directory."。
    *   如果不存在，输出 "File not found."。

---

### **准备好了吗？**
建议拿出一张纸，写下答案。**写完后回复“交卷”**，我将为你进行阅卷和最后的考前叮嘱。加油！