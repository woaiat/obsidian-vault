

---
## 第一套
### **Linux Chapter 1 & 2 Crash Test (30 Questions)**

#### **Part 1: OS Concepts & History (Chapter 1)**

1. **Which of the following describes Linux as an operating system?**
   A. Single-user, Single-tasking
   B. Single-user, Multitasking
   C. Multiuser, Multitasking
   D. Multiuser, Single-tasking

2. **The core component of the Linux OS that interacts directly with hardware is called the:**
   A. Shell
   B. Kernel
   C. Utility
   D. Terminal

3. **Which year did Linus Torvalds create the Linux kernel?**
   A. 1969
   B. 1983
   C. 1991
   D. 1973

4. **Who started the GNU Project in 1983?**
   A. Ken Thompson
   B. Dennis Ritchie
   C. Linus Torvalds
   D. Richard Stallman

5. **A terminal that consists only of a monitor and keyboard, without its own CPU or disk, is called a:**
   A. Smart Terminal
   B. Dumb Terminal
   C. Client Terminal
   D. Root Terminal

6. **Which command is used to show the system runtime and load?**
   A. `who`
   B. `date`
   C. `uptime`
   D. `time`

7. **The root user's command prompt usually ends with which symbol?**
   A. `$`
   B. `%`
   C. `#`
   D. `>`

8. **Which command clears the terminal screen and moves the cursor to the top-left?**
   A. `clear`
   B. `tput clear`
   C. `erase`
   D. Both A and B

9. **To display only the time using the `date` command, which format option should be used?**
   A. `date "+%D"`
   B. `date "+%T"`
   C. `date "+%Y"`
   D. `date "+%Time"`

10. **Which key combination sends an "End of File" (EOF) signal to log out?**
    A. Ctrl+C
    B. Ctrl+Z
    C. Ctrl+D
    D. Ctrl+L

---

#### **Part 2: File System Structure (Chapter 2)**

11. **In the Linux file system, the top-level directory is represented by:**
    A. `\` (Backslash)
    B. `.` (Dot)
    C. `/` (Forward Slash)
    D. `~` (Tilde)

12. **Which directory typically contains system configuration files?**
    A. `/bin`
    B. `/etc`
    C. `/home`
    D. `/dev`

13. **Which symbol represents the current user's home directory?**
    A. `/`
    B. `.`
    C. `..`
    D. `~`

14. **Which directory contains essential binary utilities (commands like `ls`, `cp`)?**
    A. `/lib`
    B. `/usr`
    C. `/bin`
    D. `/var`

15. **A path that starts with `/` is called a(n):**
    A. Relative Path
    B. Absolute Path
    C. Recursive Path
    D. Symbolic Path

---

#### **Part 3: Commands & File Management (Chapter 2)**

16. **Which command creates a new directory?**
    A. `newdir`
    B. `mkdir`
    C. `touch`
    D. `mkfile`

17. **Which option for `ls` displays hidden files (files starting with `.`)?**
    A. `-l`
    B. `-h`
    C. `-a`
    D. `-R`

18. **To delete a directory that is NOT empty, which command is required?**
    A. `rmdir dir_name`
    B. `rm dir_name`
    C. `rm -r dir_name`
    D. `del dir_name`

19. **Which command allows you to view file contents page by page, allowing both forward and backward navigation?**
    A. `cat`
    B. `more`
    C. `less`
    D. `head`

20. **Which wildcard matches ANY string of characters (including empty string)?**
    A. `?`
    B. `*`
    C. `.`
    D. `[]`

21. **If you run `ls file[1-3].txt`, which file will NOT be listed?**
    A. `file1.txt`
    B. `file2.txt`
    C. `file3.txt`
    D. `file10.txt`

22. **Which option for `cp` is required to copy a directory recursively?**
    A. `-f`
    B. `-i`
    C. `-r`
    D. `-a`

23. **Which option for `ls` sorts files by file size?**
    A. `-t`
    B. `-S`
    C. `-s`
    D. `-r`

---

#### **Part 4: Permissions (Chapter 2)**

24. **In the permission string `drwxr-xr--`, what does the first character `d` indicate?**
    A. It is a data file.
    B. It is a directory.
    C. It is a default file.
    D. It is a device file.

25. **What is the numeric value for "Read" permission?**
    A. 1
    B. 2
    C. 4
    D. 7

26. **Which command gives the *owner* execute permission on `script.sh`?**
    A. `chmod a+x script.sh`
    B. `chmod u+x script.sh`
    C. `chmod g+x script.sh`
    D. `chmod o+x script.sh`

27. **What does the permission `chmod 755 file` mean?**
    A. rwx for owner, rx for group, rx for others.
    B. rwx for owner, rwx for group, rx for others.
    C. rx for owner, rx for group, rx for others.
    D. rwx for owner, no access for group/others.

28. **If a user wants to *enter* (cd into) a directory, which permission is required?**
    A. Read (r)
    B. Write (w)
    C. Execute (x)
    D. Archive (a)

29. **Which command is used to rename a file?**
    A. `rn`
    B. `mv`
    C. `cp`
    D. `name`

30. **Which special file type represents a device that reads/writes blocks of data (e.g., Hard Disk)?**
    A. Character Device
    B. Block Device
    C. Socket
    D. Pipe

---

## 第二套


---

### **Linux Chapter 1 & 2: The "Expert Mode" Exam (30 Questions)**

#### **Part 1: OS Architecture & History (Deep Dive)**

1. **Which of the following statements about Linux and UNIX is TRUE?**
   A. UNIX was originally written in C++ in 1969.
   B. Linux is a Certified UNIX operating system.
   C. Linux is a "Unix-like" operating system.
   D. Linux was created by Richard Stallman.

2. **In the Linux architecture, which component interprets user commands like `ls` and `cd`?**
   A. The Kernel
   B. The Shell
   C. The Utility
   D. The Terminal

3. **What is the primary difference between a "Dumb Terminal" and a "Smart Terminal"?**
   A. Dumb terminals have a mouse; Smart terminals do not.
   B. Dumb terminals cannot connect to a server.
   C. Dumb terminals lack their own CPU and disk storage.
   D. Smart terminals run Windows, while Dumb terminals run Linux.

4. **Which standard ensures compatibility between Unix-like operating systems?** (Hint: Page 1 yellow box)
   A. GNU GPL
   B. ANSI
   C. POSIX
   D. ISO 9001

5. **When using the `date` command, which format specifier displays the date as `mm/dd/yy`?**
   A. `+%T`
   B. `+%Y`
   C. `+%D`
   D. `+%d`

---

#### **Part 2: File System & Paths (Tricky)**

6. **Which of the following is an Absolute Path?**
   A. `../home/student`
   B. `~/Documents`
   C. `/etc/passwd`
   D. `bin/ls`

7. **What does the command `cd -` (cd hyphen) do?**
   A. Changes to the home directory.
   B. Changes to the root directory.
   C. Changes to the previous (last visited) directory.
   D. Reports an error.

8. **If the current directory is `/home/student/work` and you run `cd ../..`, where do you end up?**
   A. `/home/student`
   B. `/home`
   C. `/` (Root)
   D. `/home/student/work`

9. **Which directory contains *variable* data such as log files and print spools?**
   A. `/tmp`
   B. `/etc`
   C. `/lib`
   D. `/var`

10. **The entries `.` and `..` in a directory represent:**
    A. Hidden files and System files.
    B. Current directory and Root directory.
    C. Current directory and Parent directory.
    D. Empty file and Full file.

---

#### **Part 3: Advanced Command Usage**

11. **You want to create a directory structure `/project/code/src` in one command, but `/project` does not exist yet. Which command works?**
    A. `mkdir /project/code/src`
    B. `mkdir -r /project/code/src`
    C. `mkdir -p /project/code/src`
    D. `mkdir -f /project/code/src`

12. **Which `ls` command displays files sorted by size (largest first) and includes hidden files?**
    A. `ls -as`
    B. `ls -aS`
    C. `ls -lS`
    D. `ls -S`

13. **What happens if you run `rm directory_name` (without options) on a non-empty directory?**
    A. It deletes the directory.
    B. It prompts for confirmation.
    C. It moves the directory to Trash.
    D. It displays an error: "Is a directory".

14. **You want to rename the file `report.doc` to `final.doc` but ask for confirmation if `final.doc` already exists. Which command?**
    A. `mv -f report.doc final.doc`
    B. `mv -i report.doc final.doc`
    C. `cp -i report.doc final.doc`
    D. `rn -c report.doc final.doc`

15. **Which command combination deletes a directory tree `old_data` without prompting for any confirmation?**
    A. `rmdir old_data`
    B. `rm -r old_data`
    C. `rm -f old_data`
    D. `rm -rf old_data`

---

#### **Part 4: File Types & Wildcards (Logic)**

16. **In `ls -l` output, a file starting with `b` (e.g., `brw-r--r--`) is a:**
    A. Binary file
    B. Backup file
    C. Block device file
    D. Broken link

17. **Which command lists only directories (folders) in the current location?** (Hint: Use `-F`)
    A. `ls -d`
    B. `ls -F | grep /`
    C. `ls -l | grep "^-"`
    D. `ls -dir`

18. **The wildcard command `ls file[a-c]*.txt` will match:**
    A. `filea.txt`, `fileb1.txt`, `filec_new.txt`
    B. `filed.txt`
    C. `fileabc.txt`
    D. Only `filea.txt` and `filec.txt`

19. **What does the wildcard `?` match?**
    A. Zero or more characters.
    B. Exactly one character.
    C. Any digit.
    D. The end of a line.

20. **Running `ls *` in an empty directory will:**
    A. List nothing (or show an error like "No such file").
    B. List `.` and `..`
    C. List the parent directory.
    D. Crash the shell.

---

#### **Part 5: Permissions Calculation (Hard Core)**

21. **You want to set permissions so that the Owner has full access (rwx), Group has read-only (r--), and Others have no access (---). What is the octal code?**
    A. 744
    B. 740
    C. 750
    D. 711

22. **What is the symbolic equivalent of `chmod 644 file`?**
    A. `u=rw,go=r`
    B. `u=rwx,go=r`
    C. `u=rw,g=rw,o=r`
    D. `u=r,g=r,o=r`

23. **If a user has `rw-` permission on a directory, what can they do?**
    A. They can `ls` and `cd` into it.
    B. They can `cd` into it but cannot `ls`.
    C. They can `ls` (see filenames) but cannot `cd` or access file details.
    D. They can create files in it.

24. **Which command removes write permission for the Group from `file.txt`?**
    A. `chmod g-w file.txt`
    B. `chmod g=r file.txt`
    C. `chmod -w file.txt`
    D. `chmod o-w file.txt`

25. **The default permissions for a regular file are often `rw-r--r--`. What is the octal value?**
    A. 666
    B. 644
    C. 755
    D. 777

---

#### **Part 6: Trivia & Details (The 1% Boost)**

26. **Which command allows you to view the *beginning* (first 10 lines) of a file?**
    A. `tail`
    B. `top`
    C. `head`
    D. `cat -10`

27. **The `~` symbol expands to `/home/user` only for regular users. What does `~` expand to when logged in as root?**
    A. `/`
    B. `/home`
    C. `/home/root`
    D. `/root`

28. **Which of the following commands shows a calendar?** (Bonus knowledge, common in Linux)
    A. `date`
    B. `cal`
    C. `time`
    D. `month`

29. **What is the output of `who` command?**
    A. The current user's name only.
    B. A list of all users currently logged in.
    C. A list of all user accounts on the system.
    D. The system version.

30. **Which command is used to change the user's login password?**
    A. `password`
    B. `pass`
    C. `chpass`
    D. `passwd`

---

**做完后，回复“阅卷”，我们看看你是不是真的满绩选手！**