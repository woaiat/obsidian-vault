
Q：只对正常样本进行损失计算 没有难样本区分 类别不平衡未处理
Q:
1. BACLoss只关心特征和标签是否相关 不关心这个特征是否适合KNN分类
2. BACLoss优化的是样本标签相似度矩阵 但KNN需要特征空间中的聚类结构
3. 训练时没有管FPR 导致出现部分误判并且BACLoss对此没有惩罚 测试的时候发现FPR高了的时候已经晚了
    
- [x] BACLoss增强：最终效果：retain下降九个点左右 其余轻微上升
	- [x] 同时处理正常样本和异常样本
	- [x] 采用难样本挖掘
	- [x] 引入类别权重
- [x]  特征选择策略：最终效果：retain下降到百分之十左右 ACC和其他都大幅下降
	- [x] 用KNN交叉验证准确率
	- [x] 加入分类损失使得：强制特征可分 梯度回传 特征空间重塑
	- [x] FPR惩罚

新Q:1.保留率过低 大部分特征被剔除 2.冷启动问题 前几个特征容易被高估或低估 3.KNN评估过于严格
- [x] 分阶段特征选择 前十个BACLoss 后面用KNN+BACLoss双重验证
- [x] 调整评估标准 避免过度剔除导致性能下降
 

- [x] 特征选择策略
	- [x] 互信息二次筛选：冗余特征剔除 减少特征维度
	- [x] 增加迭代次数 时间复杂度可能增加 但是不重要特征可以进一步移除
